import { FilterMetadata } from "types/app"
import { Keyboard } from "types/keyboard"

export const computeFilterMetadata = (
  allProducts: Keyboard[],
): FilterMetadata => {
  const meta = {}  

   
}
